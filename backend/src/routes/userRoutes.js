import express from "express";

const router = express.Router();

// =========== USER ROUTES ===========

// ----- Collection ------

// Create New User
router.post("/", );

// Login
router.post("/login", );

export default router;