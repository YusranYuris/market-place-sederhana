import express from "express";

const router = express.Router();

// =========== PRODUCT ROUTES ===========

// ----- Collection ------

// Get All Products
router.get("/", );

// Create New Product
router.post("/", );

// ----- Resource ------

// Get Product By ID
router.get("/:id", );

// Update Product
router.put("/:id", );

// Delete Product
router.delete("/:id", );

export default router;